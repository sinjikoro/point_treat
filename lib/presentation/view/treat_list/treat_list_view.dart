import 'package:flutter/material.dart';

/// Treat List view.
class TreatListView extends StatelessWidget {
  /// Default constructor for the [TreatListView].
  const TreatListView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
