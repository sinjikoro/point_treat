name: point_treat_view_model
description: "A new Flutter package project."
version: 0.0.1
homepage:
publish_to: none

environment:
  sdk: ^3.5.0-152
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  point_treat_model:
    path: ../point_treat_model

dev_dependencies:
  flutter_lints: ^3.0.0
  flutter_test:
    sdk: flutter
  very_good_analysis: ^5.1.0

flutter:
